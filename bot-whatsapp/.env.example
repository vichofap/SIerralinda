# Copia este archivo como ".env" y completa los valores.

# Tu clave de la API de Claude (se obtiene en https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-...

# Minutos que el bot se queda callado en un chat cuando la dueña
# responde manualmente ese chat desde su celular (opcional, por defecto 30)
PAUSA_MINUTOS=30
